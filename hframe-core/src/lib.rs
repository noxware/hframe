mod composed_area;
mod context;
mod geo;
mod id;
mod platform;
mod test_platform;
mod tree;
mod web_platform;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
